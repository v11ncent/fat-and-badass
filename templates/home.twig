{% set page = "home" %}
{% extends "partials/_layout" %}

{% block content %}
  <section aria-labelledby="home-title">
    <h1 id="home-title" class="pop-in">FAT AND BADASS</h1>
  </section>

  <script>
  fetch('/api/guild/roster')
    .then(res => res.json())
    .then(data => console.log(data))
    .catch(err => console.error('API error:', err));
  </script>
{% endblock %}